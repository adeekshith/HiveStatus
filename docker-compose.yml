version: '3.8'

services:
  hive-status:
    image: ghcr.io/adeekshith/hivestatus:latest
    build: .
    ports:
      - "3000:3000"
    environment:
      - GATUS_BASE_URL=${GATUS_BASE_URL:-https://status.deekshith.in}
    restart: unless-stopped